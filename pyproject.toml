[tool.poetry]
name = "unofficial meta threads api"
version = "0.1.0"
description = "Unoffical Threads API"
authors = ["Danie1"]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.26.0"
pytest = "^6.2.4"
pytest-asyncio = "^0.15.1"
aiohttp = "^3.8.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
